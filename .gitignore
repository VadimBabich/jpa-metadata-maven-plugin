target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### IntelliJ IDEA ###
### The whole directory: no .idea file is tracked, and IDE state is per-machine.
.idea/

### Per-machine agent permissions (absolute home paths, key filenames). Not .claude/ —
### .claude/commands/ is shared repo tooling and stays commit-eligible.
.claude/settings.local.json
*.iws
*.iml
*.ipr

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store
### Private working material, including third-party source held under licence. Publishing any
### of it is a separate, unresolved decision. Do not remove this entry.
_doc/

### Former locations of the same material; kept to catch a re-copy. docs/ is committed and
### publishable, so the entry above does not cover these.
docs/repo_usage/
docs/repository/

### Maintainer-local agent rules: names private paths, so it must never be committed.
CLAUDE.local.md
