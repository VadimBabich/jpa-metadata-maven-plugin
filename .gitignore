target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### IntelliJ IDEA ###
### The whole directory: no .idea file is tracked, and IDE state is per-machine.
.idea/

### Per-machine agent permissions (absolute home paths, key filenames). Not .claude/ —
### .claude/commands/ is shared repo tooling and stays commit-eligible.
.claude/settings.local.json
*.iws
*.iml
*.ipr

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store
### Private working material — the program paper, the ADRs, the evidence corpus and its
### manifest. Publishing any of it is a separate, unresolved decision (R-P0.1). Do not
### remove this entry.
_doc/

### Old corpus locations (relocated to _doc/ 2026-08-16); kept to catch a re-copy.
### docs/ is committed and publishable, so _doc/ above does not cover these.
docs/repo_usage/
docs/repository/
